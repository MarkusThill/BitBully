/**
 * @file MoveList.cpp
 * @brief Translation unit anchor for @ref BitBully::MoveList.
 *
 * @ref BitBully::MoveList is implemented entirely inline inside the header;
 * this file exists so the type ends up in exactly one translation unit when
 * the library is linked.
 */
#include "MoveList.h"

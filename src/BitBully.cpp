/**
 * @file BitBully.cpp
 * @brief Translation-unit anchor for the @ref BitBully::BitBully solver.
 *
 * The solver is implemented entirely inside the header; this file exists so
 * the symbols are emitted in a single translation unit when the static
 * library is linked.
 */
#include "BitBully.h"

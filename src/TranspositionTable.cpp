/**
 * @file TranspositionTable.cpp
 * @brief Translation-unit anchor for @ref BitBully::TranspositionTable.
 *
 * The cache is implemented entirely in the header; this file ensures the
 * symbols end up in one translation unit when the library is linked.
 */
#include "TranspositionTable.h"

namespace BitBully {}  // namespace BitBully
